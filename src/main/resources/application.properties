spring.application.name=padel-app

# Server configuration for Replit environment
server.port=5000
server.address=0.0.0.0

# Allow all hosts for Replit proxy/iframe setup
server.forward-headers-strategy=native
